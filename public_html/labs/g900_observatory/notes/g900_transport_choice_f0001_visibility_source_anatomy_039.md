# G900 transport choice F0001 visibility source anatomy 039

Status: `passed`

Verdict: `transport_choice_f0001_visibility_source_anatomy_039_recorded`

Anatomy status: `f0001_visibility_fingerprint_recorded_no_rule_no_promotion`

## Question

Which construction-surface features make the denied prefix edge visible, and do they differ from allowed F0001 pairs?

## Answer

039 records the source-visibility anatomy of the denied prefix edge against the five allowed F0001 pairs.

The denied edge has diagnostic source and construction-surface fingerprints.

No fingerprint is promoted to source law.

No permission channel is admitted.

No information transport is admitted.

## Pair summary

- `B C` class `prefix_terminal_edge` permission `denied_visible_edge` construction files `17` row hits `1107`
- `B D` class `skip_one_jump` permission `allowed_local_member` construction files `1` row hits `75`
- `B E` class `long_jump` permission `allowed_local_member` construction files `1` row hits `51`
- `C D` class `internal_adjacent_edge` permission `allowed_local_member` construction files `19` row hits `1298`
- `C E` class `skip_one_jump` permission `allowed_local_member` construction files `16` row hits `1050`
- `D E` class `suffix_terminal_edge` permission `allowed_local_member` construction files `73` row hits `1457`

## Fingerprint tests

- `039-F1` `source_file_hit_count` distinguishes `true` status `diagnostic_fingerprint_not_source_law`
- `039-F2` `construction_file_hit_count` distinguishes `true` status `diagnostic_fingerprint_not_source_law`
- `039-F3` `csv_row_hit_count` distinguishes `true` status `diagnostic_fingerprint_not_source_law`
- `039-F4` `construction_csv_row_hit_count` distinguishes `true` status `diagnostic_fingerprint_not_source_law`
- `039-F5` `source_class_profile` distinguishes `true` status `diagnostic_fingerprint_not_source_law`
- `039-F6` `construction_bucket_profile` distinguishes `true` status `diagnostic_fingerprint_not_source_law`
- `039-F7` `top_matched_fields` distinguishes `true` status `diagnostic_fingerprint_not_source_law`
- `039-F8` `pair_position_class` distinguishes `true` status `diagnostic_fingerprint_not_source_law`

## Counts

Pair summary rows: `6`

Source hit rows: `387`

Denied source file hits: `50`

Denied construction file hits: `17`

Denied CSV row hits: `1602`

Denied construction CSV row hits: `1107`

Allowed source file hit counts: `[3, 3, 54, 39, 238]`

Allowed construction file hit counts: `[1, 1, 19, 16, 73]`

Distinguishing feature count: `8`

Construction distinguishing feature count: `3`

Promoted source laws: `0`

Promoted families: `0`

Promoted admissions: `0`

Current admitted permission channels: `1`

Current admitted information transports: `1`

## Interpretation

038 showed that the denied edge has a distinctive visibility profile.

039 records the source anatomy behind that profile.

The diagnostic value increased.

The proof status did not.

A visibility fingerprint can aim the next search.

It cannot grant permission by itself.

## Boundary

This probe admits no new permission channel.

It admits no new information transport.

It declares no new flux witness.

It promotes no source law.

It promotes no family.

It makes no transport family claim.

It makes no global uniqueness claim.

It claims no physical transport.

It claims no physical flux.

It claims no energy flow.

It claims no force.

It claims no physics.

It does not mutate the G900 body.

It adds no vertices.

It adds no edges.

## Keeper

Visibility became a fingerprint. It still has to become a rule.
