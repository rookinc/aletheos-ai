# G900 transport choice F0001 fingerprint cause probe 040

Status: `passed`

Verdict: `transport_choice_f0001_fingerprint_cause_probe_040_recorded`

Cause-probe status: `direct_denied_only_cause_candidates_found_no_promotion`

## Question

Which construction bucket or matched field actually causes the denied prefix edge to differ from the allowed F0001 pairs?

## Answer

040 tests construction buckets, matched fields, source classes, context keywords, and numeric source-anatomy counts.

It records candidate cause handles and fingerprint features.

It promotes no cause.

It promotes no source law.

It admits no permission channel.

It admits no information transport.

## Summary

- `direct_denied_only_cause_candidates` count `3` status `not_promoted`
- `construction_denied_only_cause_candidates` count `1` status `not_promoted`
- `allowed_only_contrast_candidates` count `1` status `not_promoted`
- `count_fingerprint_features` count `13` status `not_promoted`
- `source_law_promotions` count `0` status `locked`
- `admission_promotions` count `0` status `locked`

## Direct denied-only candidates

- `matched_field:missing_role_token` denied `2` allowed `0 0 0 0 0`
- `permission_status:denied_visible_edge` denied `50` allowed `0 0 0 0 0`
- `position_class:prefix_terminal_edge` denied `1` allowed `0 0 0 0 0`

## Count fingerprints

- `context_keyword:boundary` denied `14` allowed `0 0 17 12 29`
- `context_keyword:boundary_carrier` denied `13` allowed `0 0 16 11 29`
- `context_keyword:carrier` denied `14` allowed `0 0 17 12 36`
- `context_keyword:token` denied `3` allowed `0 0 4 1 4`
- `matched_field:token` denied `2` allowed `0 0 1 1 1`
- `numeric:construction_csv_row_hit_count` denied `1107` allowed `75 51 1298 1050 1457`
- `numeric:construction_file_hit_count` denied `17` allowed `1 1 19 16 73`
- `numeric:csv_row_hit_count` denied `1602` allowed `77 53 1711 1454 2133`
- `numeric:other_file_hit_count` denied `5` allowed `0 0 7 4 98`
- `numeric:source_file_hit_count` denied `50` allowed `3 3 54 39 238`
- `source_bucket:projection_exception` denied `3` allowed `0 0 2 2 2`
- `source_class:construction_surface` denied `17` allowed `1 1 19 16 73`
- `source_class:other_surface` denied `5` allowed `0 0 7 4 98`

## Counts

Pair count: `6`

Feature count: `87`

Selected matrix feature count: `17`

Direct denied-only cause candidates: `3`

Construction denied-only cause candidates: `1`

Allowed-only contrast candidates: `1`

Count fingerprint features: `13`

Construction count fingerprint features: `11`

Promoted source laws: `0`

Promoted families: `0`

Promoted admissions: `0`

Current admitted permission channels: `1`

Current admitted information transports: `1`

## Interpretation

039 made the denied edge's visibility profile into a fingerprint.

040 asks whether the fingerprint is already a cause.

The answer is still bounded.

A denied-only feature can aim the proof search.

A count fingerprint can aim the proof search.

Neither is a source law by itself.

The source law would need to explain why the first step is forbidden from upstream construction.

## Boundary

This probe admits no new permission channel.

It admits no new information transport.

It declares no new flux witness.

It promotes no source law.

It promotes no family.

It makes no transport family claim.

It makes no global uniqueness claim.

It claims no physical transport.

It claims no physical flux.

It claims no energy flow.

It claims no force.

It claims no physics.

It does not mutate the G900 body.

It adds no vertices.

It adds no edges.

## Keeper

A fingerprint points to a cause. It is not the cause.
