# Move Verified Render Controls

Status: UI layout correction

Moved the L/R channel opacity sliders and Faces/Edges toggles from the top lab bar into the Render card.

This patch targets the current HTML, where the render selector is labeled Primary mode.
No JavaScript IDs changed.
