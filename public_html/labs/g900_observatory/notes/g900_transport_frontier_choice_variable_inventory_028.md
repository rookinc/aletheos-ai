# G900 transport frontier choice variable inventory 028

Status: `passed`

Verdict: `frontier_choice_variable_inventory_028_recorded`

Inventory status: `frontier_choice_variables_inventoried_from_rich_018b_source_conservative_quarantine_admission_locked`

## Question

What can the quarantined frontier choice apparatus see before any choice-function search?

## Answer

028 inventories the richer 018b frontier variables.

It does not choose.

It does not admit.

## Source

Candidate source path:

`labs/g900_observatory/artifacts/csv/g900_transport_deferred_gate_frontier_full_audit_018b_rows.v1.csv`

Candidate source type:

`csv`

Candidate rows found:

`71`

Candidate fields found:

`31`

Expected gate-ready frontier count:

`71`

## Patch note

The first 028 pass used the thinner 018a worklist.

This patched 028 uses the richer 018b full-audit rows.

## Candidate field inventory

Candidate field count:

`31`

Structural candidate fields:

`12`

Guarded identity/result/label-like fields:

`19`

Unknown fields:

`0`

Fields containing value `03_09`:

`2`

Fields containing value `13`:

`7`

## Field categories

- `result_or_label_guarded`: `17`
- `candidate_tuple`: `5`
- `carrier_incidence`: `2`
- `identity_index`: `2`
- `source_transport`: `2`
- `phase_transition`: `1`
- `role`: `1`
- `support_package`: `1`

## Usability classes

- `guarded_result_or_label_like`: `17`
- `candidate_choice_variable`: `12`
- `guarded_identity_index`: `2`

## Source field inventory

Construction/source CSV files inventoried:

`59`

This remains a header-level inventory for future joining and source-language expansion.


## Conservative quarantine patch

Before 029, fields containing `expected`, `replay`, `ok`, `unique`, `gate`, `receipt`, `status`, `pass`, `failed`, `failure`, `deferred`, `current`, `selector`, `hypothesis`, `admission`, or `permission` were reclassified as guarded result/label-like fields.

This prevents the choice-function search from using audit-result fields as permission evidence.

## Interpretation

Before choice can become law, the apparatus must know what choice can see.

029 may search structural and non-label-leaking fields.

Guarded result, permission, admission, gate, replay-pass, and selector fields are inventory-only unless a later artifact proves they are construction-native rather than answer labels.

## Current ledger

Admitted permission channels: `1`

Admitted information transports: `1`

Gate-ready frontier candidates: `71`

New permission channels admitted by this inventory: `0`

New information transports admitted by this inventory: `0`

## Boundary

This inventory admits no new permission channel.

It admits no new information transport.

It declares no new flux witness.

It creates no choice hypothesis.

It runs no choice-function search.

It makes no transport family claim.

It makes no global uniqueness claim.

It claims no physical transport.

It claims no physical flux.

It claims no energy flow.

It claims no force.

It claims no physics.

It does not mutate the G900 body.

It adds no vertices.

It adds no edges.

## Keeper

Before choice can become law, the apparatus must know what choice can see.
