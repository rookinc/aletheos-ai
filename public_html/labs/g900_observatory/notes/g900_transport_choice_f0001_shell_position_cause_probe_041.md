# G900 transport choice F0001 shell-position cause probe 041

Status: `passed`

Verdict: `transport_choice_f0001_shell_position_cause_probe_041_recorded`

Shell-position status: `shell_position_denied_only_candidates_found_no_promotion`

## Question

Does the denied F0001 edge fail because it is visible at the wrong shell-depth, entry side, or return-position proxy?

## Answer

041 extracts construction-surface shell-position proxies from 039.

It tests denied-only, allowed-only, and count-fingerprint features.

It records candidates.

It promotes no source law.

It admits no permission channel.

It admits no information transport.

## Summary

- `shell_position_denied_only_candidates` count `7` status `not_promoted`
- `allowed_all_absent_denied_contrasts` count `0` status `not_promoted`
- `shell_position_count_fingerprints` count `10` status `not_promoted`
- `label_like_or_position_fingerprints` count `1` status `quarantined`
- `source_law_promotions` count `0` status `locked`
- `admission_promotions` count `0` status `locked`

## Shell-position denied-only candidates

- `rail_internal_slot:05` denied `1` allowed `0 0 0 0 0`
- `rail_internal_slot:08` denied `1` allowed `0 0 0 0 0`
- `rail_internal_slot:10` denied `1` allowed `0 0 0 0 0`
- `rail_internal_slot:14` denied `1` allowed `0 0 0 0 0`
- `rail_slot_pair:04_05` denied `8` allowed `0 0 0 0 0`
- `rail_slot_pair:05_10` denied `1` allowed `0 0 0 0 0`
- `rail_slot_pair:05_14` denied `1` allowed `0 0 0 0 0`

## Shell-position count fingerprints

- `context_keyword:boundary` denied `10` allowed `0 0 11 9 22`
- `context_keyword:boundary_carrier` denied `10` allowed `0 0 11 9 20`
- `context_keyword:carrier` denied `10` allowed `0 0 11 9 24`
- `numeric_proxy:construction_csv_row_hit_count` denied `1107` allowed `75 51 1298 1050 1457`
- `numeric_proxy:construction_file_hit_count` denied `17` allowed `1 1 19 16 73`
- `numeric_proxy:csv_row_hit_count` denied `1602` allowed `77 53 1711 1454 2133`
- `numeric_proxy:source_file_hit_count` denied `50` allowed `3 3 54 39 238`
- `rail_slot_pair_has_boundary_carrier` denied `13` allowed `0 0 14 12 25`
- `shell_proxy:slot_pair_boundary` denied `10` allowed `0 0 11 9 20`
- `source_class:construction_surface` denied `17` allowed `1 1 19 16 73`

## Label-like quarantine

- `position_class:prefix_terminal_edge` denied `1` allowed `0 0 0 0 0`

## Counts

Pair count: `6`

Construction source term rows: `1048`

Shell feature count: `102`

Selected matrix feature count: `18`

Shell-position denied-only candidates: `7`

Allowed-all absent-denied contrasts: `0`

Shell-position count fingerprints: `10`

Label-like or position fingerprints: `1`

Promoted source laws: `0`

Promoted families: `0`

Promoted admissions: `0`

Current admitted permission channels: `1`

Current admitted information transports: `1`

## Interpretation

040 showed that the denied first step has candidate cause handles.

041 asks whether those handles can be read as shell-position, entry-side, carrier, incidence, gate, or return-position proxies.

The probe can sharpen the search.

It cannot open the gate.

A shell can show the lock.

It does not open it.

## Boundary

This probe admits no new permission channel.

It admits no new information transport.

It declares no new flux witness.

It promotes no source law.

It promotes no family.

It makes no transport family claim.

It makes no global uniqueness claim.

It claims no physical transport.

It claims no physical flux.

It claims no energy flow.

It claims no force.

It claims no physics.

It does not mutate the G900 body.

It adds no vertices.

It adds no edges.

## Keeper

A shell can show the lock. It does not open it.
